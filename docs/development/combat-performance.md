# 战斗实时更新优化验证

## 持续战斗数据流与 UI 修复（2026-09-23）

用户反馈多种战斗持续卡顿，简化特效也无明显帮助。本轮用正式 `local-simulation.worker.ts`、`publishLocalCombat` 和完整 Battle 界面复测持续推进的战斗。

### 原因与修改

- Worker 原先每秒调用一次完整 `view`，其中包含技能、天赋、背包、训练师和 PvP 配置。在 25 人样例中，该投影平均耗时 681 ms，战斗数据到达间隔 P95 为 719 ms。战斗窗口原本会丢弃同一场战斗的完整更新；现在观察战斗时直接省去这项计算，在开战、结束、换场、命令检查点以及关闭战斗窗口时提供完整快照。团本状态另行每秒更新，战斗帧同时报告追赶状态。
- Worker 50 ms 调度现在包含本轮计算耗时，避免计算完成后额外再等 50 ms，拉长战斗数据周期。离线结算和手动命令仍使用原来的有界计算与权威检查点。
- 每个角色、每个伤害数字原先单独创建 Drei `Html` 根节点。新伤害出现时反复遍历整棵 Three 场景，数据更新又引发许多独立 React 提交。现在整场战斗共用一个 DOM 层；标签逐帧投影使用与模型相同的插值位置和镜头。
- 模型组件只在外观、选择、阵营显示、死亡和移除等相关变化时重新协调，位置和动作从逐帧数据读取。命中特效复用组件，避免数据包到来时反复构造几何树。实时和录制回放的投影均按数据包缓存，HUD 的额外提交不再重建布局、打断位置插值。
- `spellInfo` 的百分比耗蓝直接读取职业／等级基础法力，避免每个技能重新计算整张角色属性表。装备、光环、天赋仍按原规则影响实际属性和技能；相同 100 个模拟步骤的完整状态与战斗帧哈希逐一一致。完整 `view` 的平均耗时也从 681 ms 降到 396 ms。

### 持续战斗采样

同一台机器，Chromium 无头浏览器，Vite 开发预览，1440 × 1050、DPR 1。Canvas 就绪后预热 5 秒，采集 12 秒 rAF、主线程 CPU profile、长任务和 Worker 消息；模拟全程继续运行。团战采用 MC demo 的真实 25 人队伍和卢西弗隆；野外和五人队使用独立测试角色，并增加敌人生命以覆盖完整采样时段。夹具不连接或修改存档。

| 场景 | rAF P95 | rAF 最大间隔 | 主线程长任务 | 战斗帧到达间隔 P95 |
| --- | ---: | ---: | ---: | ---: |
| 25 人精细，修复前 | 62.6 ms | 125.1 ms | 26 | 718.9 ms |
| 25 人精细，修复后 | 41.7 ms | 62.5 ms | 0 | 129.3 ms |
| 25 人简化，修复后 | 27.8 ms | 55.6 ms | 0 | 131.2 ms |
| 五人队精细，修复后独立复测 | 20.9 ms | 132.0 ms | 1 | 127.4 ms |
| 野外精细，修复后 | 20.9 ms | 27.8 ms | 0 | 125.7 ms |

25 人精细模式的 rAF P95 下降约 33%，战斗数据间隔 P95 下降约 82%。各场景在 12 秒采样中都推进约 12 秒模拟时间。五人队仍有一次新模型首次绘制时的着色器编译长任务（约 134 ms），CPU profile 定位到 WebGL `getProgramInfoLog`；本轮没有消除该类首次加载停顿。rAF 是浏览器回调间隔，简化模式的 Canvas 按需绘制，不能由 rAF 次数推断实际 GPU 帧率，也不代表所有设备稳定 60 FPS。

复现：运行 `node apps/web/scripts/serve-dungeon-preview.mjs`，打开 `/battle-live.html?mode=raid`，也可切换 `mode=dungeon` 或 `mode=solo`。浏览器 Performance 面板录制预热后的持续战斗。原始采样、截图和 CPU profile 位于本地 `.cache/battle-live-performance/`，其中 `before-raid`、`final-raid`、`final-raid-low`、`verify-dungeon`、`final-solo` 对应上表；`compute-before/after.json` 包含模拟逐步哈希。

验证：117 项相关测试通过；根项目和 Web TypeScript 检查通过；内容 manifest 已重建且 `data:check` 通过。定向 ESLint 无错误（既有 `any` 文件关闭 `no-explicit-any` 检查），保留图标 `<img>` 和录制回放 effect 依赖两项既有类型的提示。新增覆盖完整快照延后与恢复、换场、离线命令、调度周期、实时／录制投影稳定性、标签插值同步，以及等级、装备、诅咒和宠物对耗蓝的影响。

浏览器交互复查通过标签键盘选择、缩放及自动取景、精细／简化特效切换、拖动镜头及恢复自动镜头、关闭后重新打开战斗；无页面运行错误，已检查实际战斗截图。更新后需刷新游戏页面，创建使用新代码的模拟 Worker。

## 3D 骨骼与资源加载修复（2026-09-23）

本次修改共用的 `BattleHD2D` 渲染链路，适用于野外、副本、团队和竞技场，不改变模拟步长、伤害或战斗结果。

- `SkeletonUtils.clone` 会为每个网格分块克隆骨骼。人类男性身体有 56 个分块、119 根骨骼，原实现为这些分块保留 56 份相同的骨骼矩阵。`createCreatureInstance` 现在按源 skin 合并为每角色一份；不同角色、不同 skin 仍保持独立。Three 每帧只需更新和上传一份共用矩阵。
- 出场时不再为全部 20 个动作预先创建 AnimationAction 与轨道绑定，改为首次使用时创建并复用。相同皮肤只在纹理配置变化时标记上传，不再按身体分块、角色重复标记。
- 角色模型使用独立 Suspense 边界。新召唤物、变形或换外观所需的资源尚未加载时，现有战场与其他角色继续显示，待加载角色保留可选择的状态标签。
- 地形装饰和后处理组件使用 memo，避免每次生命值／时钟更新重复遍历静态组件树。其逐帧动画和画质切换仍然生效。

在同一台机器、Chromium 无头浏览器、1440 × 1050、DPR 1、精细特效下，使用 `/battle-3d.html` 的 MC 25 人场景采样。资源就绪后暂停试玩模拟，预热 3.5 秒，记录 8 秒 rAF 间隔及浏览器 CPU profile，隔离绘制成本：

| 指标 | 修复前 | 骨骼修复后 | 最终版本复测 |
| --- | ---: | ---: | ---: |
| rAF P95 间隔 | 27.8 ms | 20.9 ms | 20.9 ms |
| rAF 最大间隔 | 69.4 ms | 48.7 ms | 69.4 ms |
| 超过 50 ms 的主线程长任务 | 1 | 0 | 1 |

两次修复后采样的 P95 均降低约 25%；最终复测仍有一次长帧，不能宣称已消除所有偶发卡顿。

这是暂停模拟后的渲染采样，不能当作实时战斗端到端帧率或所有设备稳定 60 FPS 的保证。该试玩页面将 `advance` 和完整 `view` 放在主线程每 100 ms 执行；不暂停时，本次测到 636 ms 的主线程长任务，CPU 热点包括天赋、技能和属性投影。正式游戏通过 `local-simulation.worker.ts` 执行这些计算，所以不能直接用这个试玩页的运行中采样推断正式游戏绘制成本。

验证：74 项相关测试通过，Web TypeScript 检查通过；ESLint 无错误，仅保留角色标签现有的原生图片提示。新增回归验证真实模型各分块在待机、移动、攻击、施法、死亡动作下与原实现的顶点位置一致、角色间骨骼隔离、皮肤不重复上传、资源只释放一次、Strict Mode 重连和新模型加载不挂起整个战场。

```powershell
node --test apps/web/test/creature-instance.test.mjs apps/web/test/battle-hd2d-frame.test.mjs apps/web/test/battle-hd2d.test.mjs apps/web/test/battle-scene.test.mjs apps/web/test/character-models.test.mjs apps/web/test/molten-core-models.test.mjs apps/web/test/arena-battle-scene.test.mjs
node node_modules/typescript/bin/tsc --noEmit -p apps/web/tsconfig.json
```

浏览器复查覆盖野外、五人副本、5v5 竞技场和 MC 的模型显示、标签选择、视野缩放、精细／简化特效切换、手动镜头及恢复自动镜头，未出现页面运行错误；已检查四种场景截图。

## 历史验证记录

2026-09-21：属性与回放 CPU 优化的最新测量见 [战斗预计算 CPU 优化](combat-compute-performance.md)。相同口径的五人队样例预计算由 3.32 秒降到 1.61 秒；这不是整机容量压测。

2026-09-20：画面已替换为 Three.js / React Three Fiber HD-2D 场景，见 [HD-2D 战场](hd2d-battle.md)。以下 Pixi 帧率与性能采样是历史结果，不作为新渲染器的性能结论。

2026-09-18：单人默认改为预模拟与客户端回放，挂机改为批量结算。下文记录 2026-09-17 的实时实现；最新架构和验证见 [战斗运行与回放](combat-execution.md)。

日期：2026-09-17。

## 修改

- 在线战斗结算、战斗窗口轮询目标周期均为 200 ms；worker 到期扫描默认 100 ms。无在线观察者时恢复 1000 ms 结算周期。
- `/game?scope=combat` 保留公开 player 状态，仅生成战斗 view。减少反复生成和传输训练、地图、天赋、经济、策略技能列表的开销。非战斗返回完整响应；客户端不允许跨角色、跨内容版本合并。
- 轮询串行执行，有超时、卸载取消、后台暂停和前台恢复。取消信号经 Web 代理传递至游戏服务请求。完整/战斗响应的 ETag 分开。
- Pixi 完整特效最高 60 FPS、简化特效最高 30 FPS；弹道时钟在帧内推进。HUD 10 Hz，进度条与单位位置连续过渡，显示时间不回退。命中和生命值仍以权威状态为准。
- 排布按快照/缩放缓存，网格只在比例变化时重绘；统计面板跳过纯计时重渲染，策略首次展开后才挂载并保留编辑草稿。异步渲染器加载完成时立即接收最新场景。

## 测量

使用内存中的 20 级五人小队、3 个敌人，不读取玩家存档。

`node scripts/benchmark-combat-sync.mjs`，各模式预热 5 次，测量 48 次。响应体为未压缩 UTF-8 字节，时间仅包含响应构造，不包含数据库与网络：

| 指标 | 完整快照 | 战斗快照 |
| --- | ---: | ---: |
| 响应体 | 488,942 B | 44,636 B |
| 平均构造耗时 | 26.19 ms | 1.77 ms |
| P95 构造耗时 | 28.48 ms | 2.19 ms |

响应体减少约 91%，构造耗时减少约 93%。此前探索测量使用字符串长度，以上 UTF-8 计量为最终口径。

浏览器夹具：运行 `node apps/web/scripts/serve-dungeon-preview.mjs`，打开 `/combat-upgrade.html`，点击“开始 12 秒性能采样”。关闭“简化特效”，自动取景，未展开策略。在同一开发浏览器两次采样中：

| 指标 | 修改前 | 修改后 |
| --- | ---: | ---: |
| Battle React 提交次数 | 292 | 197 |
| React 总渲染耗时 | 1423 ms | 650 ms |
| 最慢 React 渲染 | 14 ms | 10 ms |
| rAF 最大间隔 | 63 ms | 32 ms |
| rAF P95 间隔 | 14 ms | 15 ms |

React 渲染耗时减少约 54%。rAF 测量是浏览器回调间隔，不等于 Pixi 实际 FPS，也不构成所有设备稳定 60 FPS 的保证。夹具仍在浏览器中运行真实引擎和完整 view，采样不代表生产网络端到端延迟。

## 验证

- `npm test`：868 项全部通过。
- 后续代理取消信号等相关回归：16 项通过（包含新增代理取消测试）。
- `npm run typecheck` 和 Web TypeScript 检查通过。
- `npm --prefix apps/web run build`：生产构建通过。
- `npm run check`、`npm run data:check` 通过，内容 manifest 已重新生成。
- 浏览器截图检查了完整特效场景；验证缩放、自动取景、单位选择、简化/完整特效切换、策略首次展开、收起再展开保留草稿、退出战斗。
- 覆盖野外与共享实例 200 ms 更新、离线退回 1 秒周期、响应体大小、结束战斗完整刷新、scope ETag、跨角色/内容版本隔离、慢请求不重叠、超时重试、取消、后台暂停和显示时钟。

## 启用

Web、游戏服务和 worker 一起更新后生效。已有运行环境若设置了 `GAME_WORKER_INTERVAL_MS=1000`，需改为 `100` 并重启 worker。此次验证没有重启用户当前运行的服务，也没有修改玩家数据；生产数据库/真实网络下的延迟和并发容量未压测。

## 真实账号冻结回归（2026-09-17）

`dungeon20` 在完整特效下开战约 4 秒后停止更新；数据库实例序号也连续停止，浏览器没有错误。高频 snapshot 在同一个 SERIALIZABLE 事务中写 presence 并读取完整角色/背包/实例，使读取轮询与 worker 结算相互冲突。修复将在线心跳独立成短事务，最多每秒更新一次（短离线窗口使用窗口的四分之一），随后单独读取快照。在线积压模拟每事务最多 20 ticks，提交中间进度后继续追赶，避免长事务反复重算；非个人生产任务保留原预算。

修复后原战斗完成；下一场完整特效战斗超过 30 秒仍持续推进，连续 8 次一秒间隔数据库采样均有新序号，模拟时间落后实时时间约 0.2–0.4 秒。此验证证明冻结恢复，不代表所有设备稳定 60 FPS。未清空账号或改写存档。

后续可将实时战斗改为服务端单实例内存推进、约 200 ms 发布快照、5 秒持久化检查点、结束时原子结算奖励。该方案尚未实现；必须同时调整指令路由、实例归属、重启恢复和结算幂等性，不能仅降低数据库写入频率。

## 单人本地执行与操作响应（2026-09-26）

本轮修正金团及 NPC 小队被排除在本地模拟之外的问题。网页开始单账号活动时，在同一事务中预留本地执行，浏览器下载 Worker 期间服务器也不抢先推进。实时施法、五人指挥、团长口令及战场指令在 Worker 调用共用规则，无需暂停、上传、服务器执行及重新 claim 的网络链路；存档仍按原会话、序号与时间边界校验。多账号实例继续使用服务器。

Worker 持有可变状态，每个 50 ms 调度不再 JSON 复制整份存档；保存时复制检查点。`advance` 与 `advanceOwned` 共用规则循环，前者继续为事务和预计算提供输入隔离。五种场景逐样本对比序列化状态，包括随机数、日志、伤害、位置与收益；没有调整 100 ms 战斗规则步长。

运行 `node scripts/benchmark-local-simulation.mjs`，Apple M4、Node v24.15.0，每种方式预热一次、测量三次。每轮推进 15 秒游戏时间、50 ms 调用周期；两种方式交替测量，完整状态哈希校验不计入耗时。下面是本机规则计算墙钟时间平均值：

| 场景 | 复制状态推进 | 独占状态推进 | 耗时减少 |
| --- | ---: | ---: | ---: |
| 野外 | 28.43 ms | 7.11 ms | 75.0% |
| 五人副本 | 723.16 ms | 566.64 ms | 21.6% |
| 25 人团本 | 4,625.44 ms | 4,120.14 ms | 10.9% |
| 5v5 竞技场 | 3,208.07 ms | 3,097.67 ms | 3.4% |
| 10v10 战场 | 51.49 ms | 4.36 ms | 91.5% |

这些数据不含战斗投影、数据库、网络、主线程与 GPU 渲染，不能换算成部署后的 FPS 或并发用户容量；竞技场差异较小，容易受系统负载影响。服务器主要减负来自移除单人战斗推进和实时操作请求，而非上表计算耗时比例。账户快照查询从每分钟约 6 次降为约 1 次，检查点每 10 秒一次保持不变，遇到遭遇边界另行提前保存。

浏览器验证使用 `serve-local-simulation-preview.mjs`，检查点响应人为延迟 2 秒。已观察到小队单体／自动打法立即更新，切换过程没有 `/api/game` 战斗命令请求；暂停与继续可以由本地引擎处理。也修正了经典模式下暂停按钮被伤害统计面板遮挡的问题。首次资源加载仍有开销，不承诺消除首次加载停顿。

本轮验证：111 项相关回归通过（分组运行，非全量测试），包括五种模式的确定性推进、检查点隔离、实时操作排队、慢保存、断网状态、会话交接及战场准备指令；根项目与 Web 类型检查、内容数据校验及最终 Web 生产构建通过。浏览器截图检查了五人本、25 人 NPC 团本和战歌峡谷；战场完成准备、倒计时、交战与中场编组操作。五人本与团本验证阶段记录到 17 次检查点保存、0 次实时操作服务器命令、0 次后台战斗推进提交。

开发预览在场景切换时出现两条 React 同步卸载 root 的警告，尚未定位；上述交互未观察到中断，因此不能据此宣称控制台无错误或所有场景卡顿均已消除。本轮未部署线上：上线需同步更新 Web、游戏服务与后台 worker，并刷新页面以加载匹配的新 Worker 和内容 manifest；生产环境帧率、端到端延迟及并发容量仍待复测。


## 单人存档与同步体积（2026-09-26）

进一步将历史战场、逐条日志与 PVP 显示事件从检查点剔除，确认响应仅回传会话与正式物品 ID 映射，避免回执表复制整份战斗状态。普通保存不再生成永久的周期结算/通知行；资产账本与实际完成事件保留。字段取舍及刷新后的展示变化见 [单人检查点的数据取舍](local-simulation.md#2026-09-26单人检查点的数据取舍)。

`node scripts/measure-local-checkpoint.mjs`，五种引擎夹具推进至 15 秒，测量 UTF-8 JSON 字节数。响应使用固定示例会话元数据且没有新物品 ID；真实 ID 长度、掉落数量和存档内容会影响结果。

| 场景 | 原状态 | 裁剪后检查点 | 单次上传加响应体积减少 |
| --- | ---: | ---: | ---: |
| 野外（已有 1 场历史） | 14,002 B | 6,191 B | 77.3% |
| 五人副本 | 52,418 B | 29,885 B | 71.4% |
| 25 人团本 | 161,612 B | 121,418 B | 62.4% |
| 5v5 竞技场 | 124,878 B | 101,828 B | 59.2% |
| 10v10 战场 | 17,430 B | 16,804 B | 51.5% |

后四个样例尚无历史战斗快照，体积收益主要来自逐条事件裁剪与省去完整状态响应。此口径不包含 HTTP 头、压缩、数据库页、WAL 或索引，不能当作线上磁盘或带宽节省比例。真实服务回归验证无新物品的检查点回执小于 1 KB，连续 12 次保存只有一条本地回执，周期结算和 outbox 行数不增长。

本轮 123 项相关测试通过（分组运行并对末次改动补测，非全量测试），覆盖五模式恢复、真实 SQL 序列化/重启/回滚、HTTP 鉴权与重复请求、掉落 ID 映射、慢 ACK 和完成事件恰好一次。根项目及 Web 类型检查、内容 manifest 校验、Web 生产构建通过。此次未重新进行浏览器画面测试，也未部署线上。

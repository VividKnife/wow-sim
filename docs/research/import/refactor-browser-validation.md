# 重构后网页短流程验收

日期：2026-09-16。通过浏览器操作 localhost:5173，使用新的验收角色“重构流程验收”，没有导入旧角色，也没有修改角色等级、金币或经验。

## 实际操作

- 新建人类法师，确认账号名册、角色、生命和法力显示。
- 接受并交付任务 783，经验增加 40。
- 发起野外狩猎，观察后台活动、自动战斗和独立战斗界面；停止活动后返回世界。
- 出售实际战利品，取得训练费用；学习奥术智慧（1459），钱包由 32 铜减少到 22 铜。
- 再次战斗和出售，最终为 1 级、240/400 经验、34 铜；刷新后角色和进度保留。
- 检查专业入口：急救因不足 100 铜而不可学习，炼金受 5 级门槛限制。这里没有通过网页实际学成专业。
- 检查坐骑入口显示 20 级解锁，1 级角色不能骑乘。
- 390×844 视口检查：页面内容宽度 375，没有横向溢出；已加载图片无破图。部分配方仍用占位图标，不能以“无破图”表述为原版图标齐全。
- 结束时控制台最近错误/警告为空，临时视口已恢复默认，验收角色页保留。

## 运行环境与边界

原 5173 服务为旧预览，本轮改用重构后的网页 + API（8788）+ worker。既有 5432 端口被占用，使用独立 Docker Compose 项目 wow-sim-audit，PostgreSQL 端口 55432；没有清空已有数据库。新建本地开发环境配置，敏感值不写入验收报告。

这是浏览器短流程、真实后台与数据库保存检查。九职业 1–20 长程使用规则引擎普通命令推进，专业和坐骑主要使用受控服务场景；不能将它们称为九职业都在网页上手动玩到 20 级。手机检查是桌面浏览器视口模拟，未验证真实 iOS/Android 后台挂起行为。刷新持久化不等于数据库崩溃恢复验收。

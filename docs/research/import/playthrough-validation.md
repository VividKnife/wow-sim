# 连续升级试玩记录（2026-09-16）

## 已验证的范围

固定代码和数据快照，从 `createGame` 创建人类法师，通过 `act` 执行正常操作、通过 `advance` 推进游戏时间，抵达 20 级。没有直接写入等级、经验、金币、物品、已学技能、生命或法力。不是浏览器点击测试，也不是完整内容验收。

独立回放从头重新创建角色，重放 43,928 条操作，并用 `assert.deepEqual` 比较完整最终状态，通过。包含 4,962 次操作命令，其余是时间推进。代码文件与数据 SHA-256、升级检查点、任务清单和结果见 [机器可读证据](playthrough-20-evidence.json)。

| 指标 | 固定快照结果 |
| --- | --- |
| 等级 | 1 → 20 |
| 完成任务 | 76 |
| 击杀 | 1,161 |
| 死亡 | 1（19 级，对手 391） |
| 游戏时间 | 60,623.878 秒，约 16 小时 50 分钟 |
| 最终货币 | 6 金 2 银 85 铜 |
| 10 / 15 / 18 级时间 | 2.47 / 7.33 / 12.13 小时 |
| 回放 | 完整状态一致 |

这次选用任务优先、普通怪优先的脚本决策：自动比较实际获得的装备、出售非任务杂物、到训练师处学习优先法术、分配火系天赋、制造食水、施加护甲和智力增益。每场战斗后通过休息和正常自然恢复回满，不能据此推算普通玩家的最佳升级速度。每个任务的游戏时间仍真实结算；宿主电脑无须等待十几个小时。

## 玩法评估与明确缺口

- 基础任务—旅行—战斗—掉落—训练—装备—恢复循环可持续推进，没有金币或补给的永久死锁。
- 18–20 级仅新增一个已完成任务，击杀由 851 增至 1,161。这条脚本路线后期明显依赖重复杀怪。需要把尚未覆盖的精英、职业事件和副本加入连续流程后再评价节奏。
- 仍有 10 个在做任务：33、102、104、155、167、168、176、1861、1920、2040。脚本暂不操作特殊职业任务道具、护送及副本流程；这些未完成不自动意味着游戏实现缺失。
- 当前策略可能推迟返回训练师；物品评分也是试玩决策规则，不是原版职业最优配装。最终只学了所选优先法术，不能宣称全部技能已测试。
- 此次为单人野外流程，未招募队友、未进入死亡矿井。副本 58 段全清、多人装备分配、职业任务完整串联仍待验证。
- 这份证据证明该代码快照的可玩性和可复现性，不证明与暴雪原版数值完全一致。社区数据库与所参考核心的版本语义、完整命中/躲闪/招架模型等仍须继续核对。

## 同期修复：战士救场目标

发现并以失败测试复现：战士技能选择脱离坦克的怪物，但移动与普通攻击仍使用第一只怪物；远处攻击法师的怪可能不被接回。

统一技能、接近移动和普通攻击的目标选择；优先救援正在攻击其他成员的怪物，并在嘲讽持续期间继续对该目标建立仇恨。保留当时并行任务加入的二维距离和策略逻辑。两条回归测试覆盖远处接怪、嘲讽后平砍及后续破甲，修改前两条都失败，修复后通过。

## 重现方式

在 `apps/web` 执行（每次使用全新输出目录）：

```powershell
node scripts/snapshot-playthrough.mjs 20 283 ../../.cache/playthrough-new-run
```

运行器复制游戏源码和公开数据到输出目录的 `runtime`，记录文件哈希，再依次试玩、独立回放。新版本拒绝复用已有目录；重新核验前将验证文件标记为未通过，避免失败运行遗留旧成功标记。

本次完整本地证据目录：`C:/workspace/wow-sim/.cache/playthrough-snapshot-20`。其中有 commands.jsonl、final-state.json、各等级存档、source-manifest.json、replay-verification.json 和执行源码快照。该目录未提交；摘要和文件哈希保存在本文件旁。

第一轮未固定快照的记录因同时进行的战斗系统修改，无法在后来的源码上回放，已被上述固定快照结果替代，不能把它当作当前版本的回放验证。后续引擎变更仍需要新的完整试玩。

## 本轮检查

- 当前工作区 `node --test test/*.test.mjs`：156/156 通过。
- `tsc --noEmit`、生产构建：通过。
- 新目录 1 级快照/回放冒烟验证通过，再次使用同目录被拒绝。
- 只读审查指出重复运行可能遗留旧验证文件，已修复并验证。
- 本轮未重新做浏览器界面测试；上述检查不能替代完整移动端流程验收。

# 九职业 1—60 级完整职业内容

用户要求直接完成九职业 1—60 级职业内容，避免后续遗漏。沿用 Classic/1.12 来源、现有确定性二维引擎和当前工作区。该授权包含实现、测试、修复；不包含重置工作区、发布或提交其他任务的修改。

## 范围与验收

覆盖 9 职业、8 种族、40 组合的等级属性与升级至60；完整技能等级目录与获得方式；27天赋树及51点构筑；种族技能、宠物/恶魔、变形、图腾、姿态、职业任务解锁与职业物品；所有效果须有实际可观察行为，不能仅将支持标记打开。

世界地图、普通区域任务和团队副本是独立世界内容，不把其缺失误报为职业已完成；职业能力需要旅行/目标类型等支持时应接入现有系统并明确可用条件。遵循 AGENTS.md，不新增旧存档迁移或兼容层。所有新增随机行为使用持久化种子；完整与分段/JSON恢复推进必须一致。

数据与执行分离：完整来源清单确保无漏项；机制按伤害、治疗、光环、触发、控制、资源、召唤、装备、旅行和交互处理。每个条目有来源、等级、获取方式、执行路径和验证状态。未完成项不能伪装成已实现，覆盖报告是验收依据而不是跳过工作的许可。

## 集成

扩展现有 classes-reference 与导入器到60级并补齐依赖、任务/物品和XP。保持既有catalog接口。战斗扩展按原有调用入口接入；天赋模块提供统一属性/法术修正及事件钩子。成长/UI解除20级限制，训练/任务/物品/种族行为保持服务端权威验证。

## 验证

建立完整目录覆盖校验、40组合60级属性/升级、九职业51点构筑、各类技能的实际效果和获取、宠物与职业机制生命周期、资源消耗、错误目标/条件、当前存档序列化及分段确定性回归；最后执行全仓测试、类型、构建和浏览器流程。

# HD-2D Battle Implementation Plan

> **For agentic workers:** Use superpowers:subagent-driven-development for isolated asset production and review. The user authorized automatic execution.

**Goal:** Ship a working HD-2D replacement for the complete battle presentation.

**Architecture:** Keep authoritative combat and replay outside the renderer. A lazy React Three Fiber canvas reads a shared presentation frame; 3D sprites, effects and projected DOM labels use identical interpolated positions.

**Tech Stack:** Next.js, React 19, Three.js, React Three Fiber, Drei, locally stored pixel-art assets.

- [x] Produce and inspect a local transparent character atlas with provenance; preserve role and species coverage.
- [x] Add failing tests for world mapping, ground presets, animation state, clocks and asset selection; implement pure presentation helpers.
- [x] Install compatible rendering dependencies and create environment, sprite animation, effects, camera and overlay modules.
- [x] Replace the old Field, remove Pixi and SVG miniature implementation, retain battle controls and accessibility.
- [x] Add a standalone real-engine preview with ground, quality, pause and encounter controls for manual QA; retain ended battle inspection and existing playback tests.
- [x] Run focused tests, typecheck, production build and desktop/mobile browser inspection; repair actual regressions.
- [x] Independently review spec compliance then code quality, record validation and final asset paths.

Verification commands: `node --test apps/web/test/battle-hd2d.test.mjs apps/web/test/battle-scene.test.mjs apps/web/test/combat-view.test.mjs apps/web/test/combat-playback.test.mjs`; `npx tsc --noEmit` and `npm run build` in `apps/web`; browser fixture served by the HD-2D preview script. Commit only task-owned files if integration requires a commit; do not include other active changes.


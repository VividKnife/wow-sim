# Nine-class level 60 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Preserve existing shared workspace edits; no commits or resets.

**Goal:** Complete Classic nine-class progression and mechanics through level 60, with an enumerated coverage ledger preventing omissions.
**Architecture:** Extend the pinned source catalog, implement reusable effect families plus class-specific behavior, integrate talent event hooks and progression/UI. Retain deterministic engine state.
**Tech Stack:** JavaScript game engine, React/TypeScript, Node tests, Python source import.
**Spec:** `docs/superpowers/specs/2026-09-16-classes-60-design.md`

## Global Constraints

- Nine classes, eight races, 40 valid combinations; levels 1–60 and 51 talent points at60.
- Source-backed mechanics; no enabling skills/talents that still have no effect.
- All randomness through serialized rngState; follow AGENTS.md: no legacy-save migration or compatibility layer.
- Preserve concurrent workspace work. No reset, unsolicited commit, or deployment.
- World geography is separate; expose real target/location constraints for utility abilities.

## Tasks and ownership

1. **Full data and acquisition inventory.** Own `docs/research/import/import-classes.py`, generated class reference/icons, `catalog.js`, and data tests. Expand every20 cap to60; include XP, supporting proc/aura/enchant records, all class/racial skills, class quest/item/book acquisition and rank chains. Retain public arrays/maps. Export `classContentManifest` with class/race, acquisition, source and all expected IDs. Test40x60 level coverage and source completeness; regenerate reproducibly.
2. **Combat and skill execution.** Own `class-mechanics.js`, combat modules, spell support portion of `class-support.js`, new focused effect modules/tests. Implement all1–60 class skill families and lifecycle behavior. Keep `decideClass(s,c,e,actors,api)` and classEffect/tick interfaces compatible; add explicit reusable talent hooks when needed after coordination. Verify damage/heal/control/resource and specialty effects, not cast logs alone.
3. **Complete talents.** Own `talent-effects.js`, new talent runtime module/tests and talent support portion of `class-support.js`. Enumerate every node from27trees. Implement stats, spell changes, proc/trigger events, active grants and prerequisites. Publish hooks with exact call signatures for combat task. Test each effect family and per-class31point capstone/51point allocations. Report any missing execution hook immediately.
4. **Growth, racial/utility/acquisition and UI integration.** Coordinator owns character/engine/progression/UI and new acquisition modules/tests. Enable60level stats/XP/equipment proficiency/51points, use the current save structure; expose real unlock flow and full catalog coverage. Implement racial utility and missing noncombat capability dependencies. Verify actual learned abilities and persisted progression.
5. **Coverage, integration and independent review.** Build manifest-to-executor reconciliation and behavior tests; resolve all missing mechanics from the full list. Run whole suite, syntax/type/build and browser checks. Record remaining world-only boundaries accurately; do not claim whole-class completion with unsupported class entries.

Each task cycle: write failing behavior/coverage tests; observe failure; implement; run tests; write result report; review actual diff and resolve findings before completion. Use task-specific files under `.cache/classes-60/` for reports/review packages. The working tree is the user’s already-authorized shared continuation; no isolated baseline reset or commit is appropriate.

## Execution ledger

- Planning/context complete. User directly authorized extension to60. Existing shared-tree edits retained.
- Data, acquisition, combat, talents and progression/UI are integrated. Generated coverage links the player spell inventory and all 432 talent nodes to execution declarations; it explicitly does not equate registry membership with individually verified original-server behavior.
- Independent integration review repaired selected-target casts, utility prerequisites, protected/foreign items, unique banked items, respec cleanup, summon-helper validation, channel lifecycle and final ticks.
- Final source review repaired active stats/resistance/regen, temporary demon behavior, hunter loyalty and training knowledge, learned pet passive stats, source Hawk proc values, and target-type/level gates.
- Ruling: follow current AGENTS.md instead of the initial plan's legacy-save assumption. No compatibility or save-migration layer is included.
- Ruling: original racial starting zones, original quest-chain geography, raids and vertical terrain routes remain world-content boundaries. AI helpers perform the required ritual participation in this simulator.
- Final acceptance: repository tests, coverage audit, syntax, TypeScript and production build are recorded in `docs/research/import/classes-60-validation.md` after the final shared-tree freeze.
- Acceptance complete: 539/539 repository tests pass; coverage audit, syntax, TypeScript, production build and diff whitespace checks pass. Browser flows verified spell learning/manufacture, actual beast taming, talent spending and paid reset. No commit, reset or deployment was performed.

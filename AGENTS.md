# Coding Agent 开发指引

## 项目阶段与基本原则

本项目目前处于初期开发阶段。以当前实际需求、结构合理性和长期可维护性为优先，允许大面积重构和破坏性变更，不需要为历史版本做任何兼容性工作。

## 重构范围

- 所有存档格式、存储结构、序列化与反序列化逻辑均可重新设计，无需兼容旧存档。
- 所有 entity 的数据、字段、类型、标识、关系及组织结构均可按实际需求新增、删除、合并、拆分或重命名。
- 所有代码、接口、模块边界、配置、静态数据及运行时数据结构均可调整或整体重构，不必维持旧接口或旧行为。
- 历史实现仅供参考，不构成新设计必须遵守的约束。若现有架构不适合当前需求，应直接调整架构。

## 实施要求

- 不要为了兼容历史版本而新增或保留兼容层、旧字段别名、双版本读写、旧格式回退、过渡接口或存档迁移逻辑。
- 旧存档或旧开发数据与新结构冲突时，可以使其失效，并通过重新创建存档、重新生成数据或重建开发数据来适配新结构，无需实现迁移。
- 重构时同步更新受影响的调用方、数据定义、相关测试、脚本和文档，移除被替代的旧实现及无用兼容代码，保持当前实现一致。
- 测试应验证当前需求和新结构；仅用于维持旧格式或旧接口兼容性的测试可以删除或改写。
- 不要因为变更范围较大或会破坏历史兼容性而退回局部补丁，也无需仅为破坏兼容性再次请求确认。
- 重构范围应服务于当前任务；不兼容历史版本不代表可以忽略当前功能正确性、数据校验或必要的验证。

除非用户在具体任务中明确提出新的兼容性要求，否则始终遵循以上原则。

## 界面规范

- 整个游戏禁止使用原生下拉框（`<select>`、`<option>` 或 NativeSelect）。使用统一的 `GameSelect`，带图标的复杂选项使用现有 Radix Select 组件；保留键盘操作、标签和禁用态。预览与测试页面也遵循此规则。
